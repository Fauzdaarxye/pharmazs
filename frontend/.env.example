# Base URL of the Node API (Contract §1). No trailing slash.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api

# Set to 1 to run the UI fully standalone against bundled fixtures (no backend).
NEXT_PUBLIC_USE_MOCKS=0
